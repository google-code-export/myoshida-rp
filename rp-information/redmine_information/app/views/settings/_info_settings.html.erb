
<h2> <%= t('label_hide_info_categories') %> </h2>
<table>
<% InfoCategory.categories.each {|catsym| %>
  <% name = "hide_#{catsym}" %>
  <tr><td>
  <%= check_box_tag("settings[#{name}]", 1, @settings[name]) %>
  <%= InfoCategory.label(catsym) %>
  </td></tr>
<% } %>
</table>
