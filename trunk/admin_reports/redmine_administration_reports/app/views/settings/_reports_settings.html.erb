
<h2> <%= t('label_hide_item') %> </h2>
<table>
<% ReportItem.items.each {|item| %>
  <% name = "hide_#{item}" %>
  <tr><td>
  <%= check_box_tag("settings[#{name}]", 1, @settings[name]) %>
  <%= ReportItem.label(item) %>
  </td></tr>
<% } %>
</table>
